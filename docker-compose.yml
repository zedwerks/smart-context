version: '3.1'
services:
  smart-context:
    image: smart-context
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8088:8088"
    volumes:
      - .:/app
    environment:
    - ISSUER=http://localhost:9200/smartauth
    - SCOPES=smart-context
    - AUDIENCE=authserver
